import 'package:flutter/material.dart';

class AudioNavBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text(
          'navbar',
        ),
      ),
    );
  }
}
